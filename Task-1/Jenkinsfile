pipeline {
    agent any
    stages {
        stage('Build Docker image') {
            steps {
                sh 'docker build -t pythonimage:1.0 .'
            }
        }
        stage('Run Docker container') {
            steps {
                sh 'docker run pythonimage:1.0'
            }
        }
    }
}
